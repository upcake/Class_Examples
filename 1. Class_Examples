
public class HelloWorld {
	
	public static void main(String[] args) {
	// main 치고 Crtl + Space 로 메인메서드 바로 입력 가능
	// Crtl + Shift + F 로 자동 들여쓰기 가능
		System.out.println("Hello World!"); // sysout 치고 컨트롤 + 스페이스로 바로 입력 가능
		// Crtl + S 그냥 저장, Crtl + Shift + S 모두 저장
		// Crtl + F11 로 바로 실행 가능
//		import 방법 
//		① File (패키지 우클릭) - import - General - Existing projects ~ => zip 파일 통째로
//		② 프로젝트만 있는게 아닌 다른 파일이 섞여있는 경우 (위와 같음) - Existing projects => project만
//		옵션 Copy projects into workspace 체크 해제
//		단, 외장 메모리에서 불러올 경우에는 체크 풀지 말것
	}

}
